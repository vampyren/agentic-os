// Placeholder — full implementation lands in v0.2.0 commit 4
// (SQLite FTS5 index + chokidar watcher).
export default function MemoryPlaceholder() {
  return (
    <div className="panel p-8 text-center">
      <h2 className="text-[18px] font-medium tracking-tight mb-2">Memory</h2>
      <p className="text-[13px] text-[var(--fg-dim)]">
        Coming in v0.2.0 commit 4.
      </p>
    </div>
  );
}
